============
Installation
============

At the command line::

    $ easy_install django-ethereum

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-ethereum
    $ pip install django-ethereum
